// Comentario inicial
programa 

variables:
numero numero1;
binario bin1;
texto tex1;
lista de numero de 10 miListaDeNumeros, miListaDeNumeros1;
lista de texto de 3 miListaDeStrings;

{
    agregar a (miListaDeNumeros, 5, 0)
    //imprimir(miListaDeNumeros)
    numero1 = quitar de (miListaDeNumeros, 0)
    //imprimir(miListaDeNumeros, numero1)
    numero1 = aleatorio(de 1, a 10)
    imprimir(numero1, aleatorio(de 1, a 10))
}