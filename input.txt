// Comentario inicial
programa 

variables:
numero numero1;
lista de numero de 1 miListaDeNumeros, miListaDeNumeros1;
lista de texto de 3 miListaDeStrings;

{
    agregar a miListaDeNumeros(5)
    imprimir(accesar(miListaDeStrings, 0))
    quitar ultimo de miListaDeNumeros()
}